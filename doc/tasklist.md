# План разработки библиотечной системы

> Итерационный план разработки. Каждая итерация добавляет новый функционал и позволяет протестировать работу приложения.
> Принцип: KISS — максимально простое решение.

---

## 📊 Отчет по прогрессу

| Итерация | Название | Статус | Прогресс | Тестирование |
|----------|----------|--------|----------|--------------|
| 1 | Настройка проекта | ✅ Завершено | 4/4 | Авторизация работает |
| 2 | Миграции и модели | ✅ Завершено | 7/7 | Структура БД создана |
| 3 | CRUD авторов | ✅ Завершено | 6/6 | Создание/просмотр авторов |
| 4 | CRUD книг | ✅ Завершено | 7/7 | Создание/просмотр книг с авторами |
| 5 | Подписка на авторов | ✅ Завершено | 4/4 | Подписка работает |
| 6 | SMS сервис | ✅ Завершено | 5/5 | SMS логируется |
| 7 | Отчеты | ⏳ В ожидании | 0/3 | Отчет ТОП-10 работает |

**Легенда статусов:**
- ⏳ В ожидании
- 🔄 В работе
- ✅ Завершено
- ❌ Блокировано

**Общий прогресс:** 33/34 задач (97%)

---

## Итерация 1: Настройка проекта

**Цель:** Настроить базовую инфраструктуру проекта (RBAC, конфигурация, Pretty URLs)

**Результат:** Авторизация работает, RBAC настроен, можно войти как пользователь

- [x] Настроить `authManager` (DbManager) в `config/web.php` и `config/console.php`
- [x] Включить Pretty URLs в `config/web.php` (`enablePrettyUrl => true`)
- [x] Применить миграции RBAC: `yii migrate --migrationPath=@yii/rbac/migrations`
- [x] Создать консольную команду `commands/RbacController.php` для инициализации ролей и permissions

**Тестирование:** Проверить авторизацию, вход пользователя, наличие роли `user` в RBAC

---

## Итерация 2: Миграции и модели

**Цель:** Создать структуру БД и модели с связями

**Результат:** Все таблицы созданы, модели работают, связи настроены

- [x] Создать миграцию `mXXXXXX_create_author_table.php`
- [x] Создать миграцию `mXXXXXX_create_book_table.php`
- [x] Создать миграцию `mXXXXXX_create_book_author_table.php`
- [x] Создать миграцию `mXXXXXX_create_author_subscription_table.php`
- [x] Применить все миграции: `yii migrate`
- [x] Создать модели через Gii: `Author`, `Book`, `BookAuthor`, `AuthorSubscription`
- [x] Настроить связи в моделях: `Author::getBooks()`, `Book::getAuthors()`, `AuthorSubscription::getAuthor()`

**Тестирование:** Проверить структуру БД, создать тестовые записи через консоль, проверить связи

---

## Итерация 3: CRUD авторов

**Цель:** Реализовать полный CRUD для авторов с AccessControl

**Результат:** Можно создавать, просматривать, редактировать и удалять авторов (только для авторизованных)

- [x] Создать `AuthorController` через Gii
- [x] Настроить AccessControl: `index`, `view` — для всех (`?`, `@`), `create`, `update`, `delete` — только для `@`
- [x] Добавить логирование операций в контроллере
- [x] Обновить меню в `views/layouts/main.php` (добавить "Авторы")
- [x] Протестировать CRUD операции (создание, просмотр, редактирование, удаление)
- [x] Протестировать доступ (гость может только просматривать, юзер — все)

**Тестирование:** Создать автора, просмотреть список, отредактировать, удалить. Проверить доступ для гостя и юзера

---

## Итерация 4: CRUD книг

**Цель:** Реализовать полный CRUD для книг с выбором авторов и загрузкой обложек

**Результат:** Можно создавать книги с выбором авторов, загружать обложки, редактировать и удалять

- [x] Создать `BookController` через Gii
- [x] Добавить в модель `Book`: поле `$coverFile` (UploadedFile), валидацию файла, метод `upload()`
- [x] Создать папку `web/uploads/covers/`
- [x] В форме книги добавить выбор авторов (чекбоксы) и поле загрузки обложки
- [x] В контроллере обработать сохранение связей `book_author` (очистить старые, вставить новые)
- [x] Настроить AccessControl (аналогично авторам)
- [x] Добавить логирование операций

**Тестирование:** Создать книгу с авторами и обложкой, просмотреть, отредактировать, удалить. Проверить загрузку файла

---

## Итерация 5: Подписка на авторов

**Цель:** Реализовать функционал подписки гостей на авторов

**Результат:** Гости могут подписаться на автора, запись создается в БД

- [x] Создать модель формы `models/forms/SubscribeForm.php` с валидацией телефона
- [x] Добавить в `AuthorController` action `actionSubscribe($id)`
- [x] Создать view для формы подписки
- [x] В `views/author/view.php` добавить форму/кнопку "Подписаться"
- [x] Обработать создание `AuthorSubscription` (проверка на дубликат)
- [x] Настроить AccessControl: доступно для всех (`?`, `@`)

**Тестирование:** Подписаться на автора как гость, проверить запись в БД, попробовать подписаться дважды

---

## Итерация 6: SMS сервис

**Цель:** Реализовать сервис отправки SMS и интегрировать его в подписку и создание книг

**Результат:** SMS логируются при подписке и при создании новой книги

- [x] Создать `services/SmsService.php` с методом `send($phone, $text)`
- [x] Настроить компонент `sms` в `config/web.php` и `config/params.php`
- [x] Реализовать эмулятор (логирование вместо реальной отправки)
- [x] Интегрировать SMS в подписку: `SmsService::sendSubscriptionConfirmation()`
- [x] Интегрировать SMS в создание книги: найти подписчиков авторов, отправить SMS каждому уникальному телефону

**Тестирование:** Подписаться на автора — проверить лог SMS. Создать книгу — проверить лог SMS для подписчиков

---

## Итерация 7: Отчеты

**Цель:** Реализовать отчет ТОП-10 авторов за год

**Результат:** Можно просмотреть отчет с ТОП-10 авторами по количеству книг за выбранный год

- [ ] Создать `ReportController` с action `actionTopAuthors($year = null)`
- [ ] Реализовать SQL-запрос для получения ТОП-10 (JOIN, GROUP BY, COUNT, ORDER BY, LIMIT)
- [ ] Создать view с таблицей (ФИО автора, количество книг)
- [ ] Настроить AccessControl: доступно для всех (`?`, `@`)
- [ ] Обновить меню (добавить "Отчет")

**Тестирование:** Открыть отчет, проверить корректность данных, проверить фильтр по году

---

## Завершение проекта

После завершения всех итераций:

- [ ] Запустить все тесты (если есть)
- [ ] Проверить логирование всех операций
- [ ] Обновить README.md с инструкциями
- [ ] Финальная проверка всех сценариев из ТЗ

---

**Последнее обновление:** 2025-01-09

---

## История изменений

- **2025-01-09**: Итерация 1 завершена (4/4 задач) — RBAC настроен, Pretty URLs включены
- **2025-01-09**: Итерация 2 завершена (7/7 задач) — Миграции и модели созданы, все связи работают, тесты пройдены
- **2025-01-09**: Итерация 3 завершена (6/6 задач) — CRUD авторов реализован, AccessControl настроен, логирование работает
- **2025-01-09**: Итерация 4 завершена (7/7 задач) — CRUD книг реализован, загрузка обложек, выбор авторов, все тесты проходят
- **2025-01-09**: Итерация 5 завершена (4/4 задач) — Подписка на авторов реализована, форма работает для всех пользователей, все тесты проходят
- **2025-01-09**: Итерация 6 завершена (5/5 задач) — SMS сервис реализован, интегрирован в подписку и создание книг, добавлены unit-тесты, все тесты проходят (79 тестов)

